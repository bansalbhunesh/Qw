# Core
openai>=1.40.0          # OpenAI-compatible client -> DashScope (Qwen, Qwen-VL)
requests>=2.31.0        # DashScope native async jobs (Wan video, TTS)
python-dotenv>=1.0.0

# Alibaba Cloud
oss2>=2.18.0            # Alibaba Cloud Object Storage Service

# Service (runs on Alibaba Cloud ECS)
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.0.0
mcp>=1.0.0             # Model Context Protocol Server

imageio-ffmpeg>=0.4.9  # static ffmpeg fallback (frame sampling + assembly, no system pkg needed)
Pillow>=10.0.0         # text rendering for the demo reel generator (scripts/make_reel.py)

# Dev / test
pytest>=8.0.0
