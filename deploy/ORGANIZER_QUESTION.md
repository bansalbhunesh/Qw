# Clarification requested from the hackathon organizers

**Where to post:** the Qwen Cloud Hackathon Devpost discussion board and/or the official
hackathon Discord/community channel. Post it as early as possible before the deadline.

---

**Subject:** Alibaba Cloud deployment proof for an India-based entrant (ECS free-tier unavailable)

> Hi — I'm an India-based solo participant on **Track 2 (AI Showrunner)**.
>
> My project's backend is built on Alibaba Cloud: it uses **Model Studio / DashScope**
> (`dashscope-intl.aliyuncs.com`) for all Qwen LLM, Qwen-VL, and Wan video calls, and
> **OSS** for reference-frame storage. I have a code file that demonstrates this
> (`deploy/alibaba_cloud.py`, `auteur/config.py`) and a live, per-call ledger
> (`out_live/ledger.json`) proving real calls to `qwen-max`, `qwen-flash`, `qwen-vl-max`,
> and `wan2.2-t2v-plus`.
>
> The rules state the deployment proof "must be a link to a code file in the repo that
> demonstrates use of Alibaba Cloud services and APIs," which I satisfy. However, the
> Alibaba Cloud **free trial (free ECS instance) is not offered for India-based individual
> accounts**, so I can't stand up a free ECS host.
>
> **Question:** Does my Model Studio/DashScope + OSS usage (with the code-file link and the
> live ledger) satisfy the Alibaba Cloud deployment-proof requirement? Or is a separate
> recording of the backend hosted on Alibaba Cloud compute (e.g., ECS/Function Compute)
> strictly required — in which case, is pay-as-you-go acceptable, and is there a
> participant credit voucher available?
>
> Thanks!

---

Keep the reply (screenshot or link) with the submission — it documents good-faith compliance
regardless of the answer.
